{
  "name": "@types/jaculus",
  "version": "0.1.0",
  "description": "Core package for JacLy providing essential functionalities.",
  "dependencies": {
    "colors": "0.1.0"
  },
  "jaculus": {
    "blocks": "blocks"
  },
  "type": "module",
  "scripts": {
    "install": "jac lib-install",
    "transpile": "npx copyfiles -u 2 src/types/*.d.ts dist/types",
    "build": "npx copyfiles -u 2 src/types/*.d.ts dist/types",
    "clean": "rm -rf dist"
  },
  "types": "dist/types/index.d.ts"
}